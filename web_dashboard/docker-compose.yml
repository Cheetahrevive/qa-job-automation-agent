version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./email_templates:/app/email_templates
    restart: unless-stopped

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
